# 템플릿 기반 페이지 생성 시스템 - 프로젝트 설계 문서

## 프로젝트 개요

### 목적
요구사항을 받으면 미리 정의된 템플릿과 컴포넌트를 조합하여 정적 웹페이지를 자동 생성하는 시스템

### 타겟 고객
- 1인 기업
- 개인 사무소 (변호사, 회계사 등)
- 소규모 비즈니스

### 핵심 기능
- YAML 설정 파일로 페이지 구성 정의
- 템플릿 기반 + 오버라이드 방식
- 완전 정적 빌드 (DB 불필요)
- 호스팅 서비스 포함 제공

---

## 기술 스택

### Frontend Framework
- **Next.js 14+** (App Router)
- **TypeScript** (엄격한 타입 안정성)
- **StyleX** (컴포넌트별 스타일 격리, zero-runtime CSS-in-JS)

### 배포
- **Vercel** (호스팅 + 자동 배포)
- 정적 빌드 (SSG)

### 주요 라이브러리
- `yaml` - YAML 파싱
- `lucide-react` - 아이콘
- `class-variance-authority` - 스타일 variants
- `clsx` - 클래스 조합

---

## 아키텍처 설계

### 1. 컴포넌트 분류 전략

**기능 중심 분류 (Functional Classification)**

```
components/
  ├── shared/              # 공통 재사용 컴포넌트
  │   ├── button/
  │   ├── container/
  │   ├── heading/
  │   ├── paragraph/
  │   ├── image/
  │   └── icon/
  │
  └── sections/            # 섹션 컴포넌트 (기능별)
      ├── hero/
      │   ├── hero-centered-image/
      │   ├── hero-full-width/
      │   └── hero-split-layout/
      ├── features/
      │   ├── features-three-column/
      │   └── features-detailed/
      ├── cta/
      │   ├── cta-simple/
      │   └── cta-split/
      ├── testimonials/
      │   ├── testimonials-grid/
      │   └── testimonials-list/
      └── contact/
          ├── contact-form/
          └── contact-info/
```

### 2. 변형(Variant) 전략

**레이아웃 구조 중심 차별화**

각 섹션 타입의 변형은 주로 **레이아웃 구조**로 구분:
- Hero: 이미지 위치, 텍스트 배치, 배경 스타일
- Features: 열 개수, 그리드 vs 리스트
- CTA: 단순형 vs 분할형
- Testimonials: 그리드 vs 리스트
- Contact: 폼 vs 정보 표시

**시각적 스타일**은 테마 시스템으로 분리

### 3. 네이밍 규칙

**설명적 이름 (Descriptive Naming)**

```typescript
// ✅ 좋은 예
HeroCenteredImage
HeroFullWidth
HeroSplitLayout
FeaturesThreeColumn
FeaturesDetailed

// ❌ 나쁜 예
Hero1, Hero2, Hero3
FeatureA, FeatureB
```

**이유:**
- 변형의 의미가 명확
- 자동완성 시 쉽게 선택
- TypeScript 타입 안정성 향상

### 4. Props 설계

**엄격한 TypeScript 타입**

```typescript
// 각 변형마다 고유한 타입 정의
export interface HeroCenteredImageProps extends BaseSectionProps {
  content: {
    headline: string;
    subheadline?: string;
    description?: string;
    cta: {
      text: string;
      href: string;
      variant?: 'primary' | 'secondary' | 'outline';
    };
    image: {
      src: string;
      alt: string;
    };
  };
}
```

**장점:**
- 컴파일 타임 오류 감지
- IDE 자동완성 지원
- 런타임 검증 최소화

### 5. 설정 파일 구조

**템플릿 + 오버라이드 방식**

```yaml
# 기본 템플릿 (templates/landing-law-firm.yml)
template:
  name: "landing-law-firm"
sections:
  - type: "hero"
    variant: "HeroCenteredImage"
    content: { ... }

# 고객 설정 (configs/client-kim.yml)
page:
  template: "landing-law-firm"
overrides:
  hero:
    headline: "커스텀 헤드라인"
    image: { ... }
```

**장점:**
- 기본 템플릿 재사용
- 고객별 커스터마이징 간편
- 설정 파일 간결함

### 6. 변형 선택 방식

**설명적 이름 기반**

```yaml
sections:
  - type: "hero"
    variant: "HeroCenteredImage"  # 명확한 이름
```

**이유:**
- 네이밍 규칙과 일관성
- YAML 작성 시 자동완성 가능
- 타입 안정성 유지

---

## MVP 범위

### 섹션 타입 및 변형 개수

| 섹션 타입 | 변형 개수 | 우선순위 | 변형 목록 |
|----------|----------|---------|-----------|
| Hero | 3개 | 🔴 필수 | HeroCenteredImage, HeroFullWidth, HeroSplitLayout |
| Features | 2개 | 🔴 필수 | FeaturesThreeColumn, FeaturesDetailed |
| CTA | 2개 | 🟡 중요 | CTASimple, CTASplit |
| Testimonials | 2개 | 🟡 중요 | TestimonialsGrid, TestimonialsList |
| Contact | 2개 | 🔴 필수 | ContactForm, ContactInfo |

**총 11개 컴포넌트**

### 페이지 타입

초기 MVP:
1. **Landing Page** (랜딩 페이지)
2. **Product/Service** (제품/서비스 소개)
3. **Contact** (연락처)

---

## 프로젝트 구조

```
template-builder/
├── src/
│   ├── components/
│   │   ├── shared/              # 공통 컴포넌트
│   │   │   ├── button/
│   │   │   │   ├── button.tsx
│   │   │   │   └── index.ts
│   │   │   ├── container/
│   │   │   ├── heading/
│   │   │   ├── paragraph/
│   │   │   ├── image/
│   │   │   └── index.ts
│   │   │
│   │   ├── sections/            # 섹션 컴포넌트
│   │   │   ├── hero/
│   │   │   │   ├── hero-centered-image/
│   │   │   │   │   ├── hero-centered-image.tsx
│   │   │   │   │   └── index.ts
│   │   │   │   ├── hero-full-width/
│   │   │   │   ├── hero-split-layout/
│   │   │   │   └── index.ts
│   │   │   ├── features/
│   │   │   ├── cta/
│   │   │   ├── testimonials/
│   │   │   ├── contact/
│   │   │   └── index.ts
│   │   │
│   │   └── registry/            # 컴포넌트 레지스트리
│   │       └── section-registry.ts
│   │
│   ├── types/                   # 전역 타입 정의
│   │   ├── section.types.ts
│   │   ├── page.types.ts
│   │   ├── theme.types.ts
│   │   └── config.types.ts
│   │
│   ├── lib/                     # 유틸리티
│   │   ├── page-builder.ts      # YAML → 페이지 변환
│   │   ├── theme-resolver.ts    # 테마 처리
│   │   └── validators.ts        # 설정 검증
│   │
│   ├── templates/               # 페이지 템플릿 정의
│   │   ├── landing-law-firm.yml
│   │   ├── landing-startup.yml
│   │   └── product-showcase.yml
│   │
│   ├── themes/                  # 테마 프리셋
│   │   ├── professional-blue.ts
│   │   ├── modern-gradient.ts
│   │   └── minimal-mono.ts
│   │
│   └── app/                     # Next.js App Router
│       ├── [slug]/
│       │   └── page.tsx         # 동적 페이지 렌더링
│       ├── layout.tsx
│       └── globals.css
│
├── public/
│   └── images/
│
├── configs/                     # 고객 설정 파일
│   ├── client-1.yml
│   └── client-2.yml
│
└── claudedocs/                  # 프로젝트 문서
    ├── PROJECT_DESIGN.md
    ├── PROTOTYPE_GUIDE.md
    ├── TYPE_DEFINITIONS.md
    ├── SAMPLE_TEMPLATES.md
    └── NEXT_SESSION.md
```

---

## 파일 네이밍 규칙

### 컴포넌트 파일
```
hero-centered-image/
  ├── hero-centered-image.tsx        # 컴포넌트 구현
  ├── hero-centered-image.types.ts   # 타입 정의 (선택적)
  ├── hero-centered-image.styles.ts  # StyleX 스타일 (선택적)
  └── index.ts                        # export
```

### 네이밍 패턴
- **폴더/파일명**: `kebab-case`
- **컴포넌트 함수**: `PascalCase`
- **타입 파일**: `*.types.ts` (선택적)
- **스타일 파일**: `*.styles.ts` (선택적, 분리가 필요한 경우)
- **유틸**: `kebab-case.ts`

---

## 워크플로우

### 고객 요청 → 페이지 생성 프로세스

1. **요구사항 수집**
   - 고객으로부터 페이지 내용 수집
   - 필요한 섹션 결정

2. **설정 파일 작성**
   ```yaml
   # configs/client-name.yml
   page:
     template: "landing-law-firm"
   overrides:
     hero:
       headline: "고객 맞춤 헤드라인"
   ```

3. **빌드 실행**
   ```bash
   npm run build
   ```

4. **배포**
   - Vercel에 자동 배포
   - 고객에게 URL 전달

---

## 구현 로드맵

### 전체 MVP: 7주 (1.5개월)

| Phase | 기간 | 내용 |
|-------|------|------|
| Phase 1 | 1주 | 프로젝트 설정 |
| Phase 2 | 1주 | 공통 컴포넌트 |
| Phase 3 | 1주 | Hero 섹션 (3개) |
| Phase 4 | 1주 | Features, CTA (4개) |
| Phase 5 | 1주 | Testimonials, Contact (4개) |
| Phase 6 | 1주 | 페이지 빌더 시스템 |
| Phase 7 | 1주 | 테스트 & 배포 |

### 프로토타입: 3-5일

**범위:**
- 1개 Hero (`HeroCenteredImage`)
- 1개 Features (`FeaturesThreeColumn`)
- 1개 Contact (`ContactForm`)
- 기본 페이지 빌더
- 1개 샘플 템플릿

---

## 확장 계획 (MVP 이후)

### 추가 섹션 타입
- Stats (통계/수치)
- FAQ (자주 묻는 질문)
- Pricing (가격표)
- Team (팀 소개)
- Portfolio (포트폴리오)

### 고급 기능
- 폼 제출 처리 (이메일, DB)
- 애니메이션 효과
- 다크 모드
- 다국어 지원

### 관리 도구
- 시각적 템플릿 편집기
- 고객 관리 대시보드
- 분석 대시보드

---

## 성능 & SEO 고려사항

### 성능 최적화
- Next.js Image 컴포넌트 활용
- 정적 빌드 (SSG)
- 코드 스플리팅
- **목표**: Lighthouse 점수 90+

### SEO
- 메타 태그 (Open Graph, Twitter Cards)
- Semantic HTML
- JSON-LD 구조화 데이터
- 빠른 로딩 속도

---

## 비즈니스 모델

### 수익 모델
- **페이지당 단가** 책정

### 호스팅
- Vercel 무료/유료 플랜 활용
- 서브도메인 제공 또는 커스텀 도메인 연결

---

## 핵심 설계 결정 요약

| 항목 | 결정사항 | 이유 |
|------|----------|------|
| 프레임워크 | Next.js 14+ | SSG, 성능, SEO, 생태계 |
| 스타일링 | StyleX | Zero-runtime, 타입 안전, 성능 |
| 컴포넌트 분류 | 기능 중심 | 재사용성, 명확한 구조 |
| 네이밍 | 설명적 이름 | 가독성, 타입 안정성 |
| Props | 엄격한 타입 | 안정성, 자동완성 |
| 설정 파일 | 템플릿+오버라이드 | 재사용성, 간결성 |
| 변형 선택 | 이름 기반 | 명확성, 일관성 |

---

## 다음 세션에서 할 일

1. **프로토타입 구현**
   - `PROTOTYPE_GUIDE.md` 참고
   - Next.js 프로젝트 생성
   - 3개 핵심 컴포넌트 구현
   - 페이지 빌더 기본 구현

2. **검증**
   - 실제 작동 확인
   - 설계 검증
   - 필요시 조정

이 문서를 기반으로 새 세션에서 즉시 구현을 시작할 수 있습니다.
